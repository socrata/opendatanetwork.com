<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="description" content="Find the data you need to power your business, app, or analysis from across the open data ecosystem." />
    <meta name="keywords" content="open data, government data, housing data, restaurant inspecitions data, transit data, health data, crime data, permit data" />
    <title>Find the data you need to power your business, app, or analysis from across the open data ecosystem.</title>
    <link href="//fonts.googleapis.com/css?family=Open+Sans:300,400,600,800" rel="stylesheet">
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
    <link href="/styles/v4-app.min.css" rel="stylesheet">
<% css.forEach(function(path) { -%>
    <link href="<%= path %>" rel="stylesheet">
<% }); -%>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="/scripts/v4-app.js"></script>
<% scripts.forEach(function(o) { if (typeof o == 'string') { -%>
    <script src="<%= o %>"></script>
<% } else { -%>
    <script src="<%= o.url %>" charset="<%= o.charset %>"></script>
<% }}); -%>
</head>
<body>
<header>
    <form method="GET" action="<%= searchPath %>" id="form" autocomplete="off">
        <a class="logo" href="/" title="Open Data Network"></a>
        <a class="fa fa-search search-link"></a>
        <span>
            <input type="text" placeholder="Search the Open Data Network" name="q" id="q" value="<%= params.q %>">
            <ul class="region-list" style="display:none"></ul>
        </span>
    </form>
</header>