<% include _header %>
<% include _search-bar %>

<script>

    // This data will be passed along to the client side scripts.
    var _data = <%- JSON.stringify({entities, variable, constraints, chartConfig, fixedConstraints, dataset}) %>;

</script>

<section class="main-section" role="main">
    <% include _region-navigation %>

    <div class="charts-column">
        <% include menus/_menus %>
        <% include menus/mobile/_menus %>
        <% include _map %>
        <div class="charts-container">
            <% include _charts %>
            <% include _citation %>

            <div id="map-container-mobile"></div>
        </div>

        <% include _search-datasets %>
    </div>
</section>

<% include _footer %>
