<ul class="chart-tabs">
    <% topics.forEach(otherTopic => { %>
        <li class="<%= otherTopic.id === topic.id ? 'selected' : '' %>">
            <a href="<%= navigate.topic(otherTopic.id).url() %>"<%- (entities.length >= 3) ? ' rel="nofollow"' : '' %>>
                <%= otherTopic.name %>
            </a>
        </li>
    <% }); %>
</ul>

